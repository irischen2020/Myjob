监听器如何工作：
1、写接口实现
2、去web.xml中注册
    <listener>
        <listener-class>com.atguigu.listener.MyServletContextListener</listener-class>
    </listener>
3、Session的生命周期：
a.第一次使用的时候创建；
    第一次访问JSP,因为session是内置使用的，所以第一次没有session，进行创建

b.销毁：两种情况，一是session自动超时失效；二是session手动设置失效了