server:
    servlet:
        context-path: /lemon
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url:
    username:
    password:

logging:
  level:
    root: info
    com.sd.mapper: trace

